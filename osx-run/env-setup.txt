cmd="$HOME/osx-run/osx-run.sh"
mkdir -p "$(dirname "$cmd")"
curl -L https://raw.githubusercontent.com/Itexoft/DevOpsKit/refs/heads/master/osx-run/osx-run.sh -o "$cmd"
chmod +x "$cmd" && $cmd

$cmd install osxcross
$cmd install python 3.11
$cmd install node 22
$cmd node -v
